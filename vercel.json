{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install --frozen-lockfile=false",
  "buildCommand": "pnpm turbo run build --filter=@tripwire/web && rm -rf .vercel/output && mv apps/web/.vercel/output .vercel/output"
}
