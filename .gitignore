# Dependencies
node_modules
/.pnpm-store
# bunx auto-generates this; we use pnpm as the package manager
bun.lock

# Env (loaded from monorepo root by @tripwire/env)
.env
*.local

# Editor / OS
.DS_Store

# Build outputs (any package or app)
dist
dist-ssr
.output
.tanstack
.nitro
.vinxi
.wrangler
.vercel
.turbo

# Tooling caches
count.txt
todos.json

# Tool-managed dirs (not source of truth)
/.agents
/.claude
/.deepsec
skills-lock.json
scripts/dump-db-to-csv.mjs
scripts/load-csv-from-db.mjs
/db-dump
